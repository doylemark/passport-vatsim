## Example usage of passport with vatsim

To run the example you must have nodejs installed

To run the example:
1. `npm install`
2. `npm start`

Server will now be running at `http://localhost:3000`
